odoo.define("owl_widget_example.WidgetKevin", function (require) {
    const FormRenderer = require("web.FormRenderer");
    const { Component } = owl;
    const { ComponentWrapper } = require("web.OwlCompatibility");


    class WidgetKevin extends Component {
     //
    };

    Object.assign(WidgetKevin, {
     template: "owl_widget_example.WidgetKevin"
    });
});